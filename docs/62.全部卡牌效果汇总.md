# Karen 全部卡牌效果汇总

> 总计 79 张卡牌（含衍生牌、诅咒牌）

本文档按卡牌类型分类，每张卡牌包含：费用/类型/稀有度/目标、关键词、中文描述、英文描述、OnPlay代码、升级效果、Shine/约定牌堆扳机等。

## 一、基础牌（Starting Deck）

### 坠落 / Fallen

**类名**: `KarenFall` | **文件**: `basic/KarenFall.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Basic |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 将{Cards:diff()}张牌放入[gold]约定牌堆[/gold]。

**英文描述**: Gain {Block:diff()} [gold]Block[/gold]. Put 1 card from your hand into the [gold]Promise Pile[/gold].

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
        await PromisePileCmd.AddFromHand(choiceContext, Owner, ((int)DynamicVars.Cards.BaseValue), this);
```

---

### 闪耀打击 / Shine Strike

**类名**: `KarenShineStrike` | **文件**: `basic/KarenShineStrike.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Basic |
| 目标 | AnyEnemy |
| Shine | 9 |

**中文描述**: 造成{Damage:diff()}点伤害。

**英文描述**: Deal {Damage:diff()} damage.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

## 二、诅咒牌

### 困意 / Sleepy

**类名**: `KarenSleepy` | **文件**: `curses/KarenSleepy.cs`

| 属性 | 值 |
|------|------|
| 费用 | -2 |
| 类型 | Curse |
| 稀有度 | Curse |
| 目标 | None |
| 关键词 | Unplayable, Ethereal, Eternal |
| 最大升级 | 0 |

**英文描述**: Unplayable. [gold]Ethereal[/gold]. Cannot be removed from your deck.

---

### 存在于舞台的理由 / Stage Reason

**类名**: `KarenStageReason` | **文件**: `curses/KarenStageReason.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Curse |
| 稀有度 | Curse |
| 目标 | Self |
| Shine | 3 |
| 关键词 | Exhaust |
| 最大升级 | 0 |

**英文描述**: [gold]Exhaust[/gold].

---

## 三、闪耀牌（Shine System）

### 背负着我们犯下的罪过 / Carrying Our Guilt

**类名**: `KarenCarryingGuilt` | **文件**: `shine/KarenCarryingGuilt.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Attack |
| 稀有度 | Rare |
| 目标 | AnyEnemy |

**中文描述**: 造成{CalculatedDamage:diff()}点伤害。 本局游戏中每耗尽一张非同名的[gold]闪耀[/gold]牌，伤害增加{ExtraDamage:diff()}点。{ShinePileUniqueCount:choose(0):| (已耗尽{ShinePileUniqueCount}张)}

**英文描述**: Deal {Damage:diff()} damage, plus {ExtraDamage:diff()} for each card in your [gold]Shine Pile[/gold].

**OnPlay 效果**:
```csharp
if (cardPlay.Target == null) return;
        await DamageCmd.Attack(DynamicVars.CalculatedDamage)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.CalculationBase.UpgradeValueBy(3m);
        DynamicVars.ExtraDamage.UpgradeValueBy(1m);
```

---

### 蓄力打击 / Charge Strike

**类名**: `KarenChargeStrike` | **文件**: `shine/KarenChargeStrike.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |
| Shine | 9 |

**中文描述**: 造成{Damage:diff()}点伤害。 目标本回合降低{StrengthPower:diff()}点[gold]力量[/gold]。

**英文描述**: Deal {Damage:diff()} damage. Apply -{StrengthPower:diff()} [gold]Strength[/gold] to the target.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        await PowerCmd.Apply<KarenChargeStrikeStrengthDownPower>(cardPlay.Target, DynamicVars.Strength.BaseValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(4m);
        DynamicVars.Strength.UpgradeValueBy(1m);
```

---

### 永无结束的命运舞台 / Endless Stage

**类名**: `KarenContinue02` | **文件**: `shine/KarenContinue02.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |
| Shine | 9 |
| 最大升级 | int |

**中文描述**: 造成{Damage:diff()}点伤害。 能被多次[gold]升级[/gold]。

**英文描述**: Deal {Damage:diff()} damage. [blue]Can be upgraded repeatedly.[/blue]

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m + _upgradeCount);
        _upgradeCount++;
```

---

### 出场 / Debut

**类名**: `KarenDebut` | **文件**: `shine/KarenDebut.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AllEnemies |
| Shine | 6 |

**中文描述**: 对所有敌人造成{Damage:diff()}点伤害{Repeat:diff()}次。

**英文描述**: Deal {Damage:diff()} damage {Repeat:diff()} times to ALL enemies.

**OnPlay 效果**:
```csharp
int hits = DynamicVars.Repeat.IntValue;
        if (CombatState == null) return;
        for (int i = 0; i < hits; i++)
        {
            await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
                .FromCard(this)
                .TargetingAllOpponents(CombatState)
                .WithHitFx(VfxCmd.slashPath)
                .Execute(choiceContext);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Repeat.UpgradeValueBy(1m);
```

---

### 水分补充 / Hydration

**类名**: `KarenDrinkWater` | **文件**: `shine/KarenDrinkWater.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| Shine | 3 |
| 关键词 | Exhaust |

**中文描述**: 阻止下次你受到的生命值损伤。

**英文描述**: Prevent the next time you would lose HP.

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<BufferPower>(Owner.Creature, 1m, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 投下燃料 / Drop Fuel

**类名**: `KarenDropFuel` | **文件**: `shine/KarenDropFuel.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| Shine | 9 |
| 关键词 | Exhaust |

**中文描述**: 抽{Cards:diff()}张牌。 获得{Energy:energyIcons()}。

**英文描述**: Draw {Cards:diff()} card(s). Gain {Energy:energyIcons()}.

**OnPlay 效果**:
```csharp
await CardPileCmd.Draw(choiceContext, DynamicVars.Cards.IntValue, Owner);
        await PlayerCmd.GainEnergy(DynamicVars.Energy.IntValue, Owner);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
        DynamicVars.Energy.UpgradeValueBy(1m);
```

---

### NONNON哒哟 / NONNON

**类名**: `KarenNonon` | **文件**: `shine/KarenNonon.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Attack |
| 稀有度 | Rare |
| 目标 | AllEnemies |
| Shine | 9 |

**中文描述**: 对所有敌人造成{Damage:diff()}点伤害。 所有敌人本回合降低{StrengthPower:diff()}点[gold]力量[/gold]。

**英文描述**: Deal {Damage:diff()} damage to ALL enemies. Apply -{StrengthPower:diff()} [gold]Strength[/gold] to ALL enemies this turn.

**OnPlay 效果**:
```csharp
if (CombatState == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .TargetingAllOpponents(CombatState)
            .WithHitFx(VfxCmd.giantHorizontalSlashPath)
            .Execute(choiceContext);

        await PowerCmd.Apply<KarenNononStrengthDownPower>(CombatState.HittableEnemies, DynamicVars.Strength.BaseValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(4m);
        DynamicVars.Strength.UpgradeValueBy(4m);
```

---

### 土豆 / Potato

**类名**: `KarenPotato` | **文件**: `shine/KarenPotato.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |
| Shine | 3 |

**中文描述**: 回复{Heal:diff()}点生命。

**英文描述**: Heal {Heal:diff()} HP.

**OnPlay 效果**:
```csharp
await CreatureCmd.Heal(Owner.Creature, DynamicVars.Heal.BaseValue);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Heal.UpgradeValueBy(3m);
```

---

### 武术练习 / Practice

**类名**: `KarenPractice` | **文件**: `shine/KarenPractice.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |
| Shine | 9 |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 [gold]闪耀[/gold]耗尽时，回复{Heal:diff()}点生命。

**英文描述**: Gain {Block:diff()} [gold]Block[/gold]. When [gold]Shine[/gold] depletes: Heal {Heal:diff()} HP.

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(4m);
        DynamicVars.Heal.UpgradeValueBy(4m);
```

**OnShineExhausted 效果**:
```csharp
if (Owner != null)
            await CreatureCmd.Heal(Owner.Creature, DynamicVars.Heal.BaseValue);
```

---

### 舞蹈练习 / KarenPractice2

**类名**: `KarenPractice2` | **文件**: `shine/KarenPractice2.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得你所有牌[gold]闪耀[/gold]值之和{IfUpgraded:show:+{CalculationBase:diff()}|}的[gold]格挡[/gold]。{InCombat: (获得{CalculatedBlock:diff()}点[gold]格挡[/gold]。)|}

**OnPlay 效果**:
```csharp
var blockAmount = (int)DynamicVars.CalculatedBlock.Calculate(cardPlay.Target);
        await CreatureCmd.GainBlock(Owner.Creature, blockAmount, ValueProp.Move, cardPlay);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.CalculationBase.UpgradeValueBy(4);
```

---

### 准备完成 / Ready

**类名**: `KarenReady` | **文件**: `shine/KarenReady.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| Shine | 6 |
| 关键词 | Innate, Exhaust |

**中文描述**: 获得{StrengthPower:diff()}点[gold]力量[/gold]。 抽1张牌。

**英文描述**: Gain {StrengthPower:diff()} [gold]Strength[/gold]. Draw 1 card.

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<StrengthPower>(Owner.Creature, DynamicVars.Strength.BaseValue, Owner.Creature, this);
        await CardPileCmd.Draw(choiceContext, 1, Owner);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Strength.UpgradeValueBy(1m);
```

---

### 用你的闪耀贯穿我吧 / Shine Through Me

**类名**: `KarenShineStrikeBarrage` | **文件**: `shine/KarenShineStrikeBarrage.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |
| Shine | 6 |

**中文描述**: 造成{Damage:diff()}点伤害{Repeat:diff()}次。

**英文描述**: Deal {Damage:diff()} damage {Repeat:diff()} times.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .WithHitCount(DynamicVars.Repeat.IntValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Repeat.UpgradeValueBy(1m);
```

---

### 星光闪耀之时 / KarenStar

**类名**: `KarenStar` | **文件**: `shine/KarenStar.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Retain |

**中文描述**: 耗尽[gold]约定牌堆[/gold]之外你唯一的[gold]闪耀[/gold]牌，将其打出同等次数。

**OnPlay 效果**:
```csharp
if (toPlay != null)
        {
            MainFile.Logger.Info($"KarenStar: To play card is {toPlay.Title}");
            // 先将这张卡移动到打出区
            await CardPileCmd.Add(toPlay, PileType.Play);
            // 增加打出次数
            toPlay.AddExtraReplayCountForNextPlay(Math.Max(0, toPlay.GetShineValueRounded() - 1));
            // 耗尽
            toPlay.SetEnterShinePileAfterPlay(true);
            // 打出这张卡
            await CardCmd.AutoPlay(choiceContext, toPlay, null);
        }
        else
        {
            MainFile.Logger.Info("KarenStar: No card to play");
        }
```

**OnUpgrade 效果**:
```csharp
AddKeyword(CardKeyword.Retain);
```

---

### 星星串起了我们的友谊 / Star Friend

**类名**: `KarenStarFriend` | **文件**: `shine/KarenStarFriend.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |
| Shine | 3 |

**中文描述**: 造成{Damage:diff()}点伤害。 [gold]斩杀[/gold]时，获得[gold]闪耀牌奖励[/gold]。

**英文描述**: Deal {Damage:diff()} damage. If this kills the target, gain a random [gold]Shine[/gold] card.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        bool shouldTriggerFatal = cardPlay.Target.Powers.All((PowerModel p) => p.ShouldOwnerDeathTriggerFatal());

        var attackCommand = await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        if (shouldTriggerFatal && attackCommand.Results.Any((DamageResult r) => r.WasTargetKilled))
        {
            await ExtraRewardCmd.AddShineCardReward(Owner, this);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(4m);
```

---

### 星光指引 / KarenStarGuide

**类名**: `KarenStarGuide` | **文件**: `shine/KarenStarGuide.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Innate, Exhaust |

**中文描述**: 将你所有的[gold]闪耀[/gold]牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
var combatState = Owner.PlayerCombatState;
        if (combatState == null) return;

        var cards = PileType.Hand.GetPile(Owner).Cards
            .Concat(PileType.Draw.GetPile(Owner).Cards)
            .Concat(PileType.Discard.GetPile(Owner).Cards)
            .Where(c => c.IsShineCard())
            .ToList();
        await PromisePileCmd.Add(Owner, cards);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 耀眼的阳光 / Sunlight

**类名**: `KarenSunlight` | **文件**: `shine/KarenSunlight.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |
| Shine | 3 |

**中文描述**: 造成{Damage:diff()}点伤害。 [gold]闪耀[/gold]耗尽时，可将此牌重新加入牌组。

**英文描述**: Deal {Damage:diff()} damage. When [gold]Shine[/gold] depletes, you may add this card to your draw pile.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(4m);
```

**OnShineExhausted 效果**:
```csharp
MainFile.Logger.Info("KarenSunlight.OnShineExhausted triggered. Prompting player to add KarenSunlight back to deck.");
        if (Owner == null)
        {
            MainFile.Logger.Error("KarenSunlight.OnShineExhausted called but Owner is null.");
            return;
        }

        // 战斗已经结束了，加入卡牌奖励
        if (!inCombat)
        {
            var player = Owner;
            var reward = new SpecialCardReward(this.CloneSafeForDeck(), player);
            if (Owner.RunState.CurrentRoom is CombatRoom combatRoom)
            {
                // 战斗房间，加入最后的奖励里
                combatRoom.AddExtraReward(Owner, reward);
                MainFile.Logger.Info("Add KarenSunlight reward to combat room's extra rewards.");
            }
            else
            {
                // 非战斗房间，直接发
                await RewardsCmd.OfferCustom(player, [reward]);
                MainFile.Logger.Info("Offered KarenSunlight reward directly to player outside of combat room.");
            }
            return;
        }

        // 还在战斗中，创建一个选择卡牌界面来让玩家选择是否将这张牌加入牌组
        // 创建一个自身的复制，需要有CombatState才能加入手牌
        CardModel clone = combatState?.CloneCard(this)!;
        if (clone == null)
        {
            MainFile.Logger.Error("KarenSunlight.OnShineExhausted failed to clone card for deck addition.");
            return;
        }

        clone.RestoreShineToMax();

        //var selected = await CardSelectCmdEx.FromChooseACardScreen(ctx, [clone], base.Owner, canSkip: true, new LocString("gameplay_ui", "KAREN_SUNLIGHT_OBTAIN_PROMPT"));
        var selected = await CardSelectCmd.FromChooseACardScreen(ctx, [clone], base.Owner, canSkip: true);

        // 选择不加入牌组
        if (selected == null)
        {
            //clone.RemoveFromState(); 
            _ = CardPileCmd.RemoveFromCombat(clone); // 不需要等待
            MainFile.Logger.Info("Player chose to skip adding KarenSunlight back to deck after shine exhaustion.");
            return;
        }

        // 选择加入牌组
        MainFile.Logger.Info("Player chose to add KarenSunlight back to deck after shine exhaustion.");
        {
            // 创建一个牌组中的牌的复制重新加入牌组
            var deckClone = clone.CloneSafeForDeck();
            if (deckClone == null)
            {
                MainFile.Logger.Error($"Failed to clone KarenSunlight deckVersion for combat shine exhaustion. Owner or RunState was null.");
                return;
            }
            deckClone.RestoreShineToMax();
            CardPileAddResult result = await CardPileCmd.Add(deckClone, PileType.Deck);
            CardCmd.PreviewCardPileAdd(result, 1.2f, CardPreviewStyle.MessyLayout);
            MainFile.Logger.Info($"Cloned KarenSunlight for combat shine exhaustion and added to deck. Clone ID: {deckClone.Id}");


            // 需要将这张牌重新放入手牌
            clone.DeckVersion = deckClone; // 关联这两张牌
            await CardPileCmd.Add(clone, PileType.Hand);
        }
```

---

### 上挑 / Sword Up

**类名**: `KarenSwordUp` | **文件**: `shine/KarenSwordUp.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |
| Shine | 9 |

**中文描述**: 造成{Damage:diff()}点伤害。 给予{WeakPower:diff()}层[gold]虚弱[/gold]和{VulnerablePower:diff()}层[gold]易伤[/gold]。

**英文描述**: Deal {Damage:diff()} damage. Apply {WeakPower:diff()} [gold]Weak[/gold] and {VulnerablePower:diff()} [gold]Vulnerable[/gold].

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target, "cardPlay.Target");

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        await PowerCmd.Apply<WeakPower>(cardPlay.Target, DynamicVars.Weak.BaseValue, Owner.Creature, this);
        await PowerCmd.Apply<VulnerablePower>(cardPlay.Target, DynamicVars.Vulnerable.BaseValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(2m);
        DynamicVars.Weak.UpgradeValueBy(1m);
        DynamicVars.Vulnerable.UpgradeValueBy(1m);
```

---

### 迈向那个舞台 / To The Stage

**类名**: `KarenToTheStage` | **文件**: `shine/KarenToTheStage.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |
| Shine | 6 |

**中文描述**: 获得{Energy:energyIcons()}。{IfUpgraded:show: 抽1张牌。|}

**英文描述**: Gain {Energy:energyIcons()}.{IfUpgraded:show: Also draw 1 card.|}

**OnPlay 效果**:
```csharp
await PlayerCmd.GainEnergy(DynamicVars.Energy.IntValue, Owner);

        if (IsUpgraded)
        {
            await CardPileCmd.Draw(choiceContext, 1, Owner);
        }
```

---

## 四、约定牌堆相关

### 水族馆 / KarenAquariumCard

**类名**: `KarenAquariumCard` | **文件**: `promisePile/KarenAquariumCard.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Power |
| 稀有度 | Uncommon |
| 目标 | Self |
| 关键词 | Innate |

**中文描述**: 每回合开始时，抽{KarenAquariumPower:diff()}张牌，然后将{KarenAquariumPower:diff()}张牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
// 给自己添加水族馆Power
        await PowerCmd.Apply<KarenAquariumPower>(
            Owner.Creature,
            DynamicVars[nameof(KarenAquariumPower)].BaseValue,
            Owner.Creature,
            this
        );
```

**OnUpgrade 效果**:
```csharp
AddKeyword(CardKeyword.Innate);
```

---

### 背靠背 / KarenBackToBackCard

**类名**: `KarenBackToBackCard` | **文件**: `promisePile/KarenBackToBackCard.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |

**中文描述**: 造成[gold]约定牌堆[/gold]中卡牌数量{ExtraDamage:diff()}倍的伤害。 {InCombat:（造成{CalculatedDamage:diff()}点伤害。）|}

**OnPlay 效果**:
```csharp
if (cardPlay.Target == null) return;
        await DamageCmd.Attack(DynamicVars.CalculatedDamage)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.ExtraDamage.UpgradeValueBy(2m);
```

---

### Banana午餐 / KarenBananaLunch

**类名**: `KarenBananaLunch` | **文件**: `promisePile/KarenBananaLunch.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Power |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: 回合结束时，增加{Cards:diff()}张[gold]三明治[/gold]到[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
// 应用Power来在每个回合结束时添加三明治
        await PowerCmd.Apply<KarenBananaLunchPower>(Owner.Creature, DynamicVars.Cards.BaseValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### 约定之塔桥 / KarenBridge

**类名**: `KarenBridge` | **文件**: `promisePile/KarenBridge.cs`

| 属性 | 值 |
|------|------|
| 费用 | X |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |

**中文描述**: 打出约定牌堆顶部X{IfUpgraded:show:+1|}张牌。

**OnPlay 效果**:
```csharp
var xValue = ResolveEnergyXValue();
        if (IsUpgraded) xValue += 1;
        if (xValue <= 0) return;

        //await CardPileCmd.AutoPlayFromDrawPile(choiceContext, base.Owner, num, CardPilePosition.Top, forceExhaust: false);
        await PromisePileCmd.AutoPlayFromPromisePile(choiceContext, base.Owner, xValue);
```

---

### 勇气打击 / KarenCourageStrike

**类名**: `KarenCourageStrike` | **文件**: `promisePile/KarenCourageStrike.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AllEnemies |

**中文描述**: 对所有敌人造成{Damage:diff()}点伤害。 将{Cards:diff()}张[gold]侧身[/gold]放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
if (CombatState == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .TargetingAllOpponents(CombatState)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        await PromisePileCmd.AddToken<KarenSideways>(Owner, CombatState, DynamicVars.Cards.IntValue);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m);
```

---

### 舞动 / KarenDance

**类名**: `KarenDance` | **文件**: `promisePile/KarenDance.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 从[gold]约定牌堆[/gold]中选择{Cards:diff()}张牌加入你的手牌。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        // 从约定牌堆选择cards张加入手牌
        await PromisePileCmd.SelectedToHand(choiceContext, Owner, (int)DynamicVars.Cards.BaseValue);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m);
        DynamicVars.Cards.UpgradeValueBy(2m);
```

---

### 闪避 / KarenDodge

**类名**: `KarenDodge` | **文件**: `promisePile/KarenDodge.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 将[gold]对峙[/gold]放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);

        // 创建对峙并放入约定牌堆
        if (CombatState != null)
        {
            await PromisePileCmd.AddToken<KarenConfront>(Owner, CombatState, DynamicVars.Cards.IntValue);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(3m);
```

---

### 命运交换之日 / Exchange Fate

**类名**: `KarenExchangeFate` | **文件**: `promisePile/KarenExchangeFate.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 将手牌与[gold]约定牌堆[/gold]中的牌交换。

**英文描述**: Exchange your hand with the cards in your [gold]Promise Pile[/gold]. [gold]Exhaust[/gold].

**OnPlay 效果**:
```csharp
await PromisePileCmd.SwitchHand(choiceContext, Owner, this);
```

**OnUpgrade 效果**:
```csharp
RemoveKeyword(CardKeyword.Exhaust);
```

---

### 神圣星辰 / Holy Star

**类名**: `KarenHolyStar` | **文件**: `promisePile/KarenHolyStar.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 被放入[gold]约定牌堆[/gold]时，本场战斗中耗能减少1。

**英文描述**: Deal {Damage:diff()} damage. When put into the [gold]Promise Pile[/gold], reduce its cost by 1 for this combat.

**OnPlay 效果**:
```csharp
if (cardPlay.Target == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue).FromCard(this).Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnAddedToPromisePile 效果**:
```csharp
EnergyCost.AddThisCombat(-1);
        MainFile.Logger.Info($"[KarenHolyStar] Reduced energy cost by 1 for this combat. Current cost: {EnergyCost.GetResolved()}");
```

---

### 落地 / KarenLanding

**类名**: `KarenLanding` | **文件**: `promisePile/KarenLanding.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AllEnemies |

**中文描述**: 对所有敌人造成{Damage:diff()}点伤害。 将[gold]回击[/gold]放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
if (CombatState == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .TargetingAllOpponents(CombatState)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        // 创建回击并放入约定牌堆
        await PromisePileCmd.AddToken<KarenCounter>(Owner, CombatState);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m);
```

---

### 重逢 / KarenMeetAgain

**类名**: `KarenMeetAgain` | **文件**: `promisePile/KarenMeetAgain.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 抽{Cards:diff()}张牌。 将1张牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
        await CardPileCmd.Draw(choiceContext, DynamicVars.Cards.IntValue, Owner);
        await PromisePileCmd.AddFromHand(choiceContext, Owner, 1, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(3m);
```

---

### 新的一天 / KarenNewDay

**类名**: `KarenNewDay` | **文件**: `promisePile/KarenNewDay.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 此牌和你打出的下{Cards:diff()}张牌进入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
// 获得格挡
        await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);

        // 应用Power来标记接下来的卡牌进入约定牌堆
        await PowerCmd.Apply<KarenNewDayPower>(Owner.Creature, DynamicVars.Cards.BaseValue, Owner.Creature, this);

        // 此牌本身进入约定牌堆
        await PromisePileCmd.Add(Owner, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(2);
        DynamicVars.Cards.UpgradeValueBy(1);
```

---

### 观察情况 / KarenNewSituation

**类名**: `KarenNewSituation` | **文件**: `promisePile/KarenNewSituation.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 将{Cards:diff()}张[gold]三明治[/gold]放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);

        // 创建三明治并放入约定牌堆
        if (CombatState != null)
        {
            await PromisePileCmd.AddToken<KarenSandwitch>(Owner, CombatState, DynamicVars.Cards.IntValue);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### 不再犹豫 / KarenNoHesitate

**类名**: `KarenNoHesitate` | **文件**: `promisePile/KarenNoHesitate.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| Shine | 9 |
| 关键词 | Exhaust |

**中文描述**: 从抽牌堆，手牌，或弃牌堆中选择{Cards:diff()}张牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
if (PromisePileManager.IsVoidMode(Owner))
        {
            await CardPileCmdEx.SelectOption(choiceContext, cardPlay, Owner, CombatState, [
                    ModelDb.Card<KarenNoHesitateHandOption>(),
                    ModelDb.Card<KarenNoHesitateDiscardPileOption>()
                        ], IsUpgraded);
        }
        else
        {
            await CardPileCmdEx.SelectOption(choiceContext, cardPlay, Owner, CombatState, [
                        ModelDb.Card<KarenNoHesitateDrawPileOption>(),
                    ModelDb.Card<KarenNoHesitateHandOption>(),
                    ModelDb.Card<KarenNoHesitateDiscardPileOption>()
                        ], IsUpgraded);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1);
```

---

### 我将再次变为我自己 / On Stage

**类名**: `KarenOnStage` | **文件**: `promisePile/KarenOnStage.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 从[gold]约定牌堆[/gold]抽{Cards:diff()}张牌。 获得{Energy:energyIcons()}。

**英文描述**: Draw 4 cards from the [gold]Promise Pile[/gold]. Gain {Energy:energyIcons()}{Energy:energyIcons()}. [gold]Exhaust[/gold].

**OnPlay 效果**:
```csharp
await PromisePileCmd.Draw(choiceContext, Owner, ((int)DynamicVars.Cards.BaseValue));
        await PlayerCmd.GainEnergy(DynamicVars.Energy.BaseValue, Owner);
```

**OnUpgrade 效果**:
```csharp
// 升级后费用变为0
        EnergyCost.UpgradeBy(-1);
```

---

### 我们的约定 / Our Promise

**类名**: `KarenOurPromise` | **文件**: `promisePile/KarenOurPromise.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |

**中文描述**: 抽{Cards:diff()}张牌。 将2张牌放入[gold]约定牌堆[/gold]。

**英文描述**: Draw {MagicNumber:diff()} cards. Put 2 cards from your hand into the [gold]Promise Pile[/gold].

**OnPlay 效果**:
```csharp
// 抽牌
        await CardPileCmd.Draw(choiceContext, base.DynamicVars.Cards.BaseValue, base.Owner);

        // 选择2张手牌放入约定牌堆
        await PromisePileCmd.AddFromHand(choiceContext, base.Owner, 2, this);
```

---

### 极速下降 / Rapid Descent

**类名**: `KarenRapid` | **文件**: `promisePile/KarenRapid.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: 从[gold]约定牌堆[/gold]中选择{Cards:diff()}张牌放入你的手牌，弃置其余。

**英文描述**: Select {Cards:diff()} card(s) from the [gold]Promise Pile[/gold]. Discard the rest.

**OnPlay 效果**:
```csharp
await PromisePileCmd.SelectedToHand(choiceContext, Owner, DynamicVars.Cards.IntValue);
        await PromisePileCmd.DiscardAll(choiceContext, Owner);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### RevueDuet / KarenRevueDuet

**类名**: `KarenRevueDuet` | **文件**: `promisePile/KarenRevueDuet.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 打出后进入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        // 将此牌放入约定牌堆
        await PromisePileCmd.Add(Owner, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m);
```

---

### 行动 / KarenRun

**类名**: `KarenRun` | **文件**: `promisePile/KarenRun.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: 抽{Cards:diff()}张牌。 打出后进入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await CardPileCmd.Draw(choiceContext, DynamicVars.Cards.IntValue, Owner);
        await PromisePileCmd.Add(Owner, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### 舞台正在等待着 / KarenStageIsWaiting

**类名**: `KarenStageIsWaiting` | **文件**: `promisePile/KarenStageIsWaiting.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Power |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: [gold]约定牌堆[/gold]被清空时，获得{Energy:energyIcons()}。

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<KarenStageIsWaitingPower>(Owner.Creature, DynamicVars.Energy.BaseValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 命运舞台的再生产 / KarenStageReproduce

**类名**: `KarenStageReproduce` | **文件**: `promisePile/KarenStageReproduce.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Power |
| 稀有度 | Rare |
| 目标 | Self |

**中文描述**: 以无限的[gold]续演[/gold]充满[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await PromisePileCmd.EnterMode(Owner, PromisePileMode.InfiniteReinforcement);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 拉伸 / Stretching

**类名**: `KarenStretching` | **文件**: `promisePile/KarenStretching.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 打出后进入[gold]约定牌堆[/gold]。

**英文描述**: Gain {Block:diff()} [gold]Block[/gold]. Put this card into the [gold]Promise Pile[/gold].

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
        await PromisePileCmd.Add(Owner, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(4m);
```

---

### 东京塔下 / KarenUnderTower

**类名**: `KarenUnderTower` | **文件**: `promisePile/KarenUnderTower.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |
| 关键词 | Exhaust |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 将1张[gold]约定之塔[/gold]放入手牌。

**OnPlay 效果**:
```csharp
// 获得格挡
        await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);

        // 生成一张约定之塔
        if (CombatState != null)
        {
            var card = CombatState.CreateCard<KarenTowerOfPromise>(Owner);
            // 加入手牌
            await CardPileCmd.AddGeneratedCardToCombat(card, PileType.Hand, addedByPlayer: true);
        }
```

---

### 世界上最空虚的人 / KarenVoid

**类名**: `KarenVoid` | **文件**: `promisePile/KarenVoid.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Power |
| 稀有度 | Rare |
| 目标 | Self |

**中文描述**: 摧毁抽牌堆，然后以[gold]约定牌堆[/gold]代替抽牌堆。

**OnPlay 效果**:
```csharp
var combatState = Owner.PlayerCombatState;
        if (combatState == null) return;

        if (PromisePileManager.IsInMode(Owner, PromisePileMode.Void))
        {
            MainFile.Logger.Info($"KarenVoid: Already in Void mode. No action taken.");
            return;
        }

        // 消耗抽牌堆
        // 一瞬间消耗所有卡牌 去看看本体卡牌的实现
        MainFile.Logger.Info($"KarenVoid: Exhausting all cards in draw pile. Count: {combatState.DrawPile.Cards.Count}");
        var drawPileCards = combatState.DrawPile.Cards.ToList();
        await Task.WhenAll(drawPileCards.Select(card => CardCmd.Exhaust(choiceContext, card)));

        // 取出约定牌堆中的所有牌 然后重新放入抽牌堆
        await CardPileCmd.Add(PromisePileManager.GetPromisePile(Owner).Cards.ToList(), PileType.Draw);

        // 切换模式
        await PromisePileCmd.EnterMode(Owner, PromisePileMode.Void);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 双人舞 / Pas de Deux

**类名**: `KarenWhosPromise` | **文件**: `promisePile/KarenWhosPromise.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 从[gold]约定牌堆[/gold]抽{Cards:diff()}张牌，然后放入{Cards:diff()}张牌。

**英文描述**: Deal {Damage:diff()} damage. Draw {MagicNumber:diff()} card(s) from the [gold]Promise Pile[/gold]. Put {MagicNumber:diff()} card(s) from your hand into the [gold]Promise Pile[/gold].

**OnPlay 效果**:
```csharp
// 造成伤害
        if (cardPlay.Target != null)
        {
            await DamageCmd.Attack(DynamicVars.Damage.BaseValue).FromCard(this).Targeting(cardPlay.Target)
                .WithHitFx(VfxCmd.slashPath)
                .Execute(choiceContext);
        }

        // 从约定牌堆抽牌
        await PromisePileCmd.Draw(choiceContext, Owner, (int)DynamicVars.Cards.BaseValue);
        // 放回
        await PromisePileCmd.AddFromHand(choiceContext, Owner, (int)DynamicVars.Cards.BaseValue, this);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(2m);
        DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### 昨天夜空的光辉 / KarenYesterglow

**类名**: `KarenYesterglow` | **文件**: `promisePile/KarenYesterglow.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Common |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 将弃牌堆中的{Cards}张牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        await PromisePileCmd.AddFromDiscard(choiceContext, Owner, DynamicVars.Cards.IntValue);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(1m);
        DynamicVars.Cards.UpgradeValueBy(1m);
```

---

## 五、力量/临时力量

### Banana松饼 / KarenBananaMuffin

**类名**: `KarenBananaMuffin` | **文件**: `strength/KarenBananaMuffin.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |

**中文描述**: 获得{StrengthPower:diff()}点[gold]临时力量[/gold]。 抽{Cards:diff()}张牌。

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<KarenBananaMuffinTempStrengthPower>(
            Owner.Creature,
            DynamicVars.Strength.BaseValue,
            Owner.Creature,
            this
        );

        await CardPileCmd.Draw(choiceContext, DynamicVars.Cards.IntValue, Owner);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Cards.UpgradeValueBy(1m);
```

---

### 一起吃饭吧 / KarenEatTogether

**类名**: `KarenEatTogether` | **文件**: `strength/KarenEatTogether.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Common |
| 目标 | Self |

**中文描述**: 获得{StrengthPower:diff()}点[gold]临时力量[/gold]。 [gold]保留力量[/gold]{Turns}回合。

**OnPlay 效果**:
```csharp
// 获得临时力量（本回合+3，下回合-3）
        await PowerCmd.Apply<KarenEatTogetherTempStrengthPower>(
            Owner.Creature,
            DynamicVars.Strength.BaseValue,
            Owner.Creature,
            this
        );

        // 获得保留力量（回合结束时不会消失）
        await PowerCmd.Apply<KarenRetainTmpStrengthPower>(
            Owner.Creature,
            DynamicVars["Turns"].IntValue,
            Owner.Creature,
            this
        );
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Strength.UpgradeValueBy(1m);
```

---

## 六、中性牌

### Banana蛋糕 / Banana Cake

**类名**: `KarenBananaCake` | **文件**: `neutral/KarenBananaCake.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 获得一瓶[gold]肌肉药水[/gold]。

**英文描述**: Gain a [gold]Flex Potion[/gold].

**OnPlay 效果**:
```csharp
await PotionCmd.TryToProcure<FlexPotion>(Owner);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 燃烧吧燃烧吧 / KarenBurn

**类名**: `KarenBurn` | **文件**: `neutral/KarenBurn.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Power |
| 稀有度 | Rare |
| 目标 | Self |

**中文描述**: 从[gold]约定牌堆[/gold]抽出的牌将被[gold]升级[/gold]，且被打出时[gold]消耗[/gold]。

**OnPlay 效果**:
```csharp
if (Owner.Creature == null) return;

        await PromisePileManager.EnterMode(Owner, PromisePileMode.Burn);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 以觉悟的名义 / KarenConsciousness

**类名**: `KarenConsciousness` | **文件**: `neutral/KarenConsciousness.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |
| Shine | 9 |
| 格挡 | 是 |

**中文描述**: 造成{Damage:diff()}点伤害。 获得等量于所造成伤害的[gold]格挡[/gold]。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        var result = await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);

        var blockAmount = result.Results.Sum((DamageResult r) => r.TotalDamage + r.OverkillDamage);
        await CreatureCmd.GainBlock(Owner.Creature, blockAmount, ValueProp.Move, cardPlay);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(3m);
```

---

### 添麻烦了 / KarenCry

**类名**: `KarenCry` | **文件**: `neutral/KarenCry.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 此牌在牌堆之间移动时，获得{Block:diff()}点[gold]格挡[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(2m);
```

---

### 觉醒形态 / KarenForm

**类名**: `KarenForm` | **文件**: `neutral/KarenForm.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Power |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Ethereal |

**中文描述**: 每个回合开始时，抽满手牌。

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<KarenFormPower>(Owner.Creature, 1m, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
RemoveKeyword(CardKeyword.Ethereal);
```

---

### 命运的齿轮 / KarenGeer

**类名**: `KarenGeer` | **文件**: `neutral/KarenGeer.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 复制所有手牌到[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await PromisePileManager.CopyHandToPromisePile(Owner);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### 最后的台词 / KarenLastWord

**类名**: `KarenLastWord` | **文件**: `neutral/KarenLastWord.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Attack |
| 稀有度 | Rare |
| 目标 | AllEnemies |

**中文描述**: [gold]约定牌堆[/gold]之外只有这张牌时才能打出。 对所有敌人造成{Damage:diff()}点伤害。

**OnPlay 效果**:
```csharp
if (!Condition(base.Owner, this)) { return; }
        if (CombatState == null) return;

        await DamageCmd.Attack(base.DynamicVars.Damage.BaseValue).FromCard(this).TargetingAllOpponents(CombatState)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(9000m);
```

---

### 明年也要在这里相见 / KarenOldPlace

**类名**: `KarenOldPlace` | **文件**: `neutral/KarenOldPlace.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 选择：保留你的能量，手牌，[gold]格挡[/gold]，或[gold]力量[/gold]{Turns:diff()}回合。

**OnPlay 效果**:
```csharp
// 获得格挡
        await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);


        await CardPileCmdEx.SelectOption(choiceContext, cardPlay, Owner, CombatState, [
            ModelDb.Card<KarenOldPlaceRetainBlockOption>(),
            ModelDb.Card<KarenOldPlaceRetainEnergyOption>(),
            ModelDb.Card<KarenOldPlaceRetainStrengthOption>(),
            ModelDb.Card<KarenOldPlaceRetainHandOption>(),
            ], IsUpgraded);
```

**OnUpgrade 效果**:
```csharp
DynamicVars["Turns"].UpgradeValueBy(1);
```

---

### 招架 / KarenParry

**类名**: `KarenParry` | **文件**: `neutral/KarenParry.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 获得或失去手牌时，本回合格挡值增加{Increase:diff()}。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block.BaseValue, ValueProp.Move, cardPlay);

        // 打出后重置（仅本回合有效）
        Reset();
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(3m);
```

---

### 披萨 / KarenPizza

**类名**: `KarenPizza` | **文件**: `neutral/KarenPizza.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | AllEnemies |
| 关键词 | Exhaust |

**中文描述**: 获得{StrengthPower:diff()}点[gold]力量[/gold]，所有敌人失去{StrengthPower:diff()}点[gold]力量[/gold]。

**OnPlay 效果**:
```csharp
// 玩家获得力量
        await PowerCmd.Apply<StrengthPower>(Owner.Creature, DynamicVars.Strength.BaseValue, Owner.Creature, this);

        // 所有敌人失去力量
        var enemies = CombatState?.HittableEnemies;
        if (enemies != null)
        {
            await PowerCmd.Apply<StrengthPower>(enemies, -DynamicVars.Strength.BaseValue, Owner.Creature, this);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Strength.UpgradeValueBy(1);
```

---

### Position 0 / KarenPosition0

**类名**: `KarenPosition0` | **文件**: `neutral/KarenPosition0.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Power |
| 稀有度 | Ancient |
| 目标 | Self |

**中文描述**: 每回合{KarenPosition0Power:choose(1):第1|前{KarenPosition0Power:diff()}}次造成攻击伤害时，获得等量[gold]格挡[/gold]。

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<KarenPosition0Power>(Owner.Creature, DynamicVars[nameof(KarenPosition0Power)].IntValue, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

## 七、遗物相关卡牌

### 骄傲 / KarenArrogant

**类名**: `KarenArrogant` | **文件**: `relic/KarenArrogant.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Power |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Innate, Ethereal |

**中文描述**: 给予自身{VulnerablePower:diff()}层[gold]易伤[/gold]。 战斗结束时，获得一个随机遗物。

**OnPlay 效果**:
```csharp
// 给予自身易伤
        var power = await PowerCmd.Apply<VulnerablePower>(base.Owner.Creature, base.DynamicVars.Vulnerable.BaseValue, base.Owner.Creature, this);
        if (power != null) power.SkipNextDurationTick = false;

        // 应用Power来在战斗结束时获得遗物
        await ExtraRewardCmd.AddRelicReward(Owner);
```

**OnUpgrade 效果**:
```csharp
// 减少易伤层数
        DynamicVars.Vulnerable.UpgradeValueBy(-1);
```

---

### Banana费南雪 / KarenFinancier

**类名**: `KarenFinancier` | **文件**: `relic/KarenFinancier.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Power |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: 获得{StrengthPower:diff()}点[gold]力量[/gold]。 每两回合[gold]保留力量[/gold]1回合。

**OnPlay 效果**:
```csharp
// 获得力量
        await PowerCmd.Apply<StrengthPower>(Owner.Creature, DynamicVars.Strength.BaseValue, Owner.Creature, this);

        // 应用保留力量的Power
        await PowerCmd.Apply<KarenFinancierPower>(Owner.Creature, 1, Owner.Creature, this);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-1);
```

---

### "宽恕" / KarenForgive

**类名**: `KarenForgive` | **文件**: `relic/KarenForgive.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Attack |
| 稀有度 | Rare |
| 目标 | AllEnemies |

**中文描述**: [gold]禁用[/gold]{DisableRelic:diff()}个遗物，然后对所有敌人造成{Damage:diff()}点伤害。

**OnPlay 效果**:
```csharp
await DisableRelicCmd.DisableRelic(Owner, DisableRelicVar.IntValue);

        // 对所有敌人造成伤害
        if (CombatState == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .TargetingAllOpponents(CombatState)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
//DisableRelicVar.UpgradeValueBy(-1);
        DynamicVars.Damage.UpgradeValueBy(7);
```

---

### 嫉妒 / KarenKillAll

**类名**: `KarenKillAll` | **文件**: `relic/KarenKillAll.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Rare |
| 目标 | AnyEnemy |
| Shine | 9 |

**中文描述**: 造成{Damage:diff()}点伤害。 [gold]闪耀[/gold]耗尽时，获得一个随机遗物。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        // 造成伤害
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(5m);
```

**OnShineExhausted 效果**:
```csharp
await ExtraRewardCmd.AddRelicReward(Owner);
```

---

### 激情 / KarenPassion

**类名**: `KarenPassion` | **文件**: `relic/KarenPassion.cs`

| 属性 | 值 |
|------|------|
| 费用 | 2 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| Shine | 1 |

**中文描述**: 战斗结束时，获得一个随机遗物。

**OnPlay 效果**:
```csharp
await ExtraRewardCmd.AddRelicReward(Owner);
```

**OnUpgrade 效果**:
```csharp
EnergyCost.UpgradeBy(-2);
```

---

### 摘星 / KarenPickStar

**类名**: `KarenPickStar` | **文件**: `relic/KarenPickStar.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: [gold]禁用[/gold]{DisableRelic:diff()}个遗物，然后获得{Energy:energyIcons()}。

**OnPlay 效果**:
```csharp
await DisableRelicCmd.DisableRelic(Owner, DisableRelicVar.IntValue);
        // 获得能量
        await PlayerCmd.GainEnergy((int)DynamicVars.Energy.BaseValue, Owner);
```

**OnUpgrade 效果**:
```csharp
DisableRelicVar.UpgradeValueBy(-1);
```

---

### 星罪 / KarenStarCrime

**类名**: `KarenStarCrime` | **文件**: `relic/KarenStarCrime.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: [gold]禁用[/gold]{DisableRelic:diff()}个遗物，然后抽{Cards:diff()}张牌。

**OnPlay 效果**:
```csharp
await DisableRelicCmd.DisableRelic(Owner, DisableRelicVar.IntValue);
        // 抽牌
        await CardPileCmd.Draw(choiceContext, (int)DynamicVars.Cards.BaseValue, Owner);
```

**OnUpgrade 效果**:
```csharp
DisableRelicVar.UpgradeValueBy(-1);
```

---

## 八、全局移动监听

### 下一个舞台 / KarenNextStage

**类名**: `KarenNextStage` | **文件**: `globalMove/KarenNextStage.cs`

| 属性 | 值 |
|------|------|
| 费用 | 3 |
| 类型 | Skill |
| 稀有度 | Uncommon |
| 目标 | Self |

**中文描述**: {IfUpgraded:show:此牌在牌堆之间移动时|此牌在被放入或离开[gold]约定牌堆[/gold]时}，抽{Cards:diff()}张牌。

**OnPlay 效果**:
```csharp
// 无打出效果
```

**OnAddedToPromisePile 效果**:
```csharp
if (!IsUpgraded)
        {
            await TriggerEffect();
        }
```

**OnGlobalMove 效果**:
```csharp
if (IsUpgraded)
        {
            await TriggerEffect();
        }
```

---

### 旋转 / KarenSpin

**类名**: `KarenSpin` | **文件**: `globalMove/KarenSpin.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Uncommon |
| 目标 | AnyEnemy |

**中文描述**: 造成{Damage:diff()}点伤害。 此牌在牌堆之间移动时，在本场战斗中伤害增加{Increase:diff()}。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars[IncreaseVarName].UpgradeValueBy(1m);
```

**OnGlobalMove 效果**:
```csharp
// 增加伤害
        decimal increase = DynamicVars[IncreaseVarName].BaseValue;
        BuffDamage(increase);
        MainFile.Logger.Info($"[KarenSpin] 牌堆移动: {from} -> {to}，伤害+{increase}，当前总伤害: {DynamicVars.Damage.BaseValue}");
```

---

### 唤醒 / KarenWakeUp

**类名**: `KarenWakeUp` | **文件**: `globalMove/KarenWakeUp.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Skill |
| 稀有度 | Rare |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 从抽牌堆{IfUpgraded:show:，[gold]约定牌堆[/gold]，或弃牌堆|}中选择{Cards:diff()}张牌放入你的手牌。

**OnPlay 效果**:
```csharp
if (IsUpgraded)
        {
            if (CombatState != null)
            {
                if (PromisePileManager.IsVoidMode(Owner))
                {
                    await CardPileCmdEx.SelectOption(choiceContext, cardPlay, Owner, CombatState, [
                    ModelDb.Card<KarenWakeUpDrawPileOption>(),
                    ModelDb.Card<KarenWakeUpDiscardPileOption>()
                    ]);
                }
                else
                {
                    await CardPileCmdEx.SelectOption(choiceContext, cardPlay, Owner, CombatState, [
                    ModelDb.Card<KarenWakeUpDrawPileOption>(),
                    ModelDb.Card<KarenWakeUpPromisePileOption>(),
                    ModelDb.Card<KarenWakeUpDiscardPileOption>()
                    ]);
                }
            }
        }
        else
        {
            await CardPileCmdEx.SelectFromDrawPileToHand(choiceContext, Owner, 2);
        }
```

---

## 九、先古卡牌

### 为什么要离开？我的朋友 / KarenWhy

**类名**: `KarenWhy` | **文件**: `ancient/KarenWhy.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Ancient |
| 目标 | Self |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。 将至多{Cards:diff()}张牌放入[gold]约定牌堆[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
        await PromisePileCmd.AddFromHand(choiceContext, Owner, ((int)DynamicVars.Cards.BaseValue), this, true);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(3m);
        DynamicVars.Cards.UpgradeValueBy(1m);
```

---

## 十、衍生牌（Token）

### 对峙 / KarenConfront

**类名**: `KarenConfront` | **文件**: `token/KarenConfront.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Token |
| 目标 | AnyEnemy |
| 关键词 | Exhaust |

**中文描述**: 造成{Damage:diff()}点伤害{Repeat:diff()}次。

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);

        int hits = DynamicVars.Repeat.IntValue;
        for (int i = 0; i < hits; i++)
        {
            await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
                .FromCard(this)
                .Targeting(cardPlay.Target)
                .WithHitFx(VfxCmd.slashPath)
                .Execute(choiceContext);
        }
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Repeat.UpgradeValueBy(1m);
```

---

### 续演 / Continue

**类名**: `KarenContinue` | **文件**: `token/KarenContinue.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Attack |
| 稀有度 | Token |
| 目标 | AnyEnemy |
| 关键词 | Exhaust |

**中文描述**: 造成{Damage:diff()}点伤害。

**英文描述**: Deal {Damage:diff()} damage.

**OnPlay 效果**:
```csharp
ArgumentNullException.ThrowIfNull(cardPlay.Target);
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
            .FromCard(this)
            .Targeting(cardPlay.Target)
            .WithHitFx(VfxCmd.slashPath)
            .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(2);
```

---

### 回击 / KarenCounter

**类名**: `KarenCounter` | **文件**: `token/KarenCounter.cs`

| 属性 | 值 |
|------|------|
| 费用 | 1 |
| 类型 | Attack |
| 稀有度 | Token |
| 目标 | AllEnemies |
| 关键词 | Exhaust |

**中文描述**: 对所有敌人造成{Damage:diff()}点伤害三次。

**OnPlay 效果**:
```csharp
if (CombatState == null) return;
        await DamageCmd.Attack(DynamicVars.Damage.BaseValue)
                .FromCard(this)
                .WithHitCount(3)
                .TargetingAllOpponents(CombatState)
                .WithHitFx(VfxCmd.slashPath)
                .Execute(choiceContext);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Damage.UpgradeValueBy(2m);
```

---

### 三明治 / KarenSandwitch

**类名**: `KarenSandwitch` | **文件**: `token/KarenSandwitch.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Token |
| 目标 | Self |
| 关键词 | Exhaust |

**中文描述**: 获得{StrengthPower:diff()}点[gold]临时力量[/gold]。

**OnPlay 效果**:
```csharp
await PowerCmd.Apply<KarenSandwitchTempStrengthPower>(
            Owner.Creature,
            DynamicVars[nameof(StrengthPower)].BaseValue,
            Owner.Creature,
            this
        );
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Strength.UpgradeValueBy(3m);
```

---

### 侧身 / KarenSideways

**类名**: `KarenSideways` | **文件**: `token/KarenSideways.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Token |
| 目标 | Self |
| 关键词 | Exhaust |
| 格挡 | 是 |

**中文描述**: 获得{Block:diff()}点[gold]格挡[/gold]。

**OnPlay 效果**:
```csharp
await CreatureCmd.GainBlock(Owner.Creature, DynamicVars.Block, cardPlay);
```

**OnUpgrade 效果**:
```csharp
DynamicVars.Block.UpgradeValueBy(6m);
```

---

### 约定之塔 / Tower of Promise

**类名**: `KarenTowerOfPromise` | **文件**: `token/KarenTowerOfPromise.cs`

| 属性 | 值 |
|------|------|
| 费用 | 0 |
| 类型 | Skill |
| 稀有度 | Token |
| 目标 | Self |
| 关键词 | Retain, Exhaust |

**中文描述**: 从[gold]约定牌堆[/gold]抽牌直到手牌上限。

**英文描述**: [gold]Retain[/gold]. Draw from the [gold]Promise Pile[/gold] until your hand is full. [gold]Exhaust[/gold].

**OnPlay 效果**:
```csharp
await PromisePileCmd.DrawToFullHand(choiceContext, Owner);
```

**OnUpgrade 效果**:
```csharp
RemoveKeyword(CardKeyword.Exhaust);
```

---

