# MCP 双版本批量测试结果（2026-09-24）

历史执行命令：`tools/test_dev_matrix.ps1 -Suite full -UpdateMcp`（当时 `full` 尚包含可选的跨版本调查；当前脚本已将其移出 `full`）。MCP 远端 HEAD 与本地已提交版本均为 `c38f912`；构建使用本地当前工作区源码，包含未提交的设置接口改动。构建副本源码 SHA-256：`553B852FA0CD4D6243055605638541BC5FFB21E5E010BA670E6879C9D4355020`。正式版 v0.107.1、测试版 v0.111.0 各自构建均为 0 警告、0 错误；两版启动与 MCP HTTP 检查通过。

| 套件 | 通过／总数 | 结果 |
| --- | --- | --- |
| 启动检查 | 2/2 | 两版正确加载 Karen，MCP 端口 15627/15628 响应 |
| 单人 | 2/2 | Karen 开局、设置接口读写后恢复、CMD 生成牌、清场后闪耀打击耗尽 |
| 同版本基础联机 | 8/8 | 两版各四种主客角色组合；双方出牌、生命同步、进入第 2 回合 |
| 联机机制回归 | 14/14 | 两版清场耗尽、混合角色《旋转》全局移牌、Karen/Karen 约定牌堆抽回 |
| 跨版本联机 | 0/2 | 两个方向均被游戏网络协议拒绝，客户端 `NErrorPopup.LocStringFromNetError` 抛出 `SwitchExpressionException`，未正常显示错误提示 |

按当前验收范围，**26 项必测全部通过**。历史运行额外测试的跨版本联机为 0/2，故当时批量脚本以退出码 1 结束；用户已明确跨版本联机不属于必测范围，当前 `full` 不再运行该套件。跨版本错误弹窗的异常保留为单独的调查记录。

调整后重新执行 `tools/test_dev_matrix.ps1 -Suite full`，结果为 **26/26 通过、退出码 0**，报告 `artifacts/test-matrix/20260924-023743-afe0ff/report.json`。报告中跨版本用例数为 0，客户端临时游戏副本已清理。

用户放行连接后，又执行 `-Suite cross` 和 `-Suite multiplayer` 作对照。跨版本仍为 **0/2**，同版本四种主客角色组合在正式版和测试版均通过（**8/8**）。两个跨版本方向的主机日志都记录到客户端握手包，说明请求到达主机；正式版主机＋测试版客户端随后握手超时，测试版客户端的错误弹窗遇到未覆盖的错误值 `13`；测试版主机＋正式版客户端的断开原因是 `106`，正式版客户端的错误弹窗同样因该值抛出 `SwitchExpressionException`。因此，放行连接没有改变跨版本结果，当前阻碍在跨版本握手／错误显示，不能归因为端口未开放。

复测报告：`artifacts/test-matrix/20260924-022015-bb1a69/report.json`（跨版本）与 `artifacts/test-matrix/20260924-022130-79d903/report.json`（同版本）；双方日志位于各自的 `logs/`。两次客户端临时副本均已清理。

本机详细报告：`artifacts/test-matrix/20260924-014124-13ef2d/report.json`，同目录 `logs/` 有 50 份主客与单人日志。客户端临时游戏副本已清理。`artifacts/` 不纳入 Git；在合并或清理工作树前，应把需要长期保留的报告和日志复制到目标工作区。
